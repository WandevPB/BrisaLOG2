# BrisaLOG Portal - Configurações de Ambiente
# Copie este arquivo para .env e configure conforme necessário

# Porta do servidor (Railway define automaticamente)
PORT=3000

# Chave secreta para JWT (OBRIGATÓRIO: altere em produção)
JWT_SECRET=brisalog_secret_key_2025_production_railway_deploy

# URL do banco de dados (SQLite para Railway)
DATABASE_URL="file:./prisma/dev.db"

# Configurações de E-mail (SendGrid - Recomendado para produção)
EMAIL_SERVICE=SendGrid
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=apikey
EMAIL_PASS=sua_api_key_do_sendgrid_aqui

# URL do Frontend (importante para links nos emails)
FRONTEND_URL=https://seu-projeto.railway.app

# Configurações de upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=uploads

# Configurações de e-mail (para implementação futura)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@brisanet.com.br

# Configurações de CORS
CORS_ORIGIN=*

# Ambiente
NODE_ENV=development

# Configurações de sessão
SESSION_TIMEOUT=24h

# Configurações de logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Configurações de backup (para implementação futura)
BACKUP_ENABLED=false
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30
